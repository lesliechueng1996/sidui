---
description: Monorepo layout and the idiom feature as the template for new work
alwaysApply: true
---

# Feature template

Treat **idiom** as the reference when adding a feature. Copy its layers, not ad-hoc shortcuts.

## Call chain

UI route → `apps/ui/src/lib/api/*` Eden wrapper → `@sidui/api-client` → `apps/api` `interface/endpoint` → `application/service` → `infrastructure/repository` → `@sidui/db`

Do not skip those layers: route handlers stay thin; pages never call Eden/`fetch` directly; services never return Elysia `status()`.

**Domain is optional.** Simple admin CRUD (table in/out, uniqueness, “in use” checks) stays in the service — do not add `domain/model/<feature>/`. Add domain only when the feature has real rules (parsing, game logic, multi-step invariants). Idiom has domain; school-style admin CRUD does not.

## Apps and packages

| Path | Package | Role |
|------|---------|------|
| `apps/api` | `@sidui/api` | Elysia HTTP API (`@api/*` → `src/*`) |
| `apps/ui` | `@sidui/ui` | TanStack Router + React Query (`@/*` → `src/*`) |
| `packages/db` | `@sidui/db` | Drizzle schema and `createClient` |
| `packages/api-client` | `@sidui/api-client` | Eden Treaty client typed from `App` |

## New feature checklist

1. Tables in `packages/db/schema/<feature>.ts`, re-export from `schema/index.ts`, then `db:generate` (ask before `db:migrate`)
2. Domain in `apps/api/src/domain/model/<feature>/` — only when there are real domain rules (skip for simple admin CRUD)
3. Repository in `apps/api/src/infrastructure/repository/`
4. Service in `apps/api/src/application/service/`
5. Elysia schemas in `apps/api/src/interface/schema/`
6. Routes on `apiRoute.group('/<resource>')`, `.use()` in `index.ts`, OpenAPI tag in `app.ts`
7. UI wrappers in `lib/api/` (admin CRUD vs user feature)
8. Admin CRUD under `routes/_authenticated/admin/`
9. User page under `routes/_authenticated/<area>/`
10. Nav entry in `routes/_authenticated/-lib/nav-items.ts` (`requiredRole` for role-gated items)

Reference: `idiom-route.ts`, `admin/idioms/`, `game-assist/guess-idiom/`.
