---
description: Database migration workflow after schema changes
globs: packages/db/schema/**
alwaysApply: false
---

After modifying database schema files:

1. `bun run --filter @sidui/db db:generate` — generate migration SQL from schema changes
2. **MUST ask the user for confirmation before running `db:migrate`** — never apply migrations without explicit approval
3. `bun run --filter @sidui/db db:migrate` — apply migrations to the database (only after user confirms)
